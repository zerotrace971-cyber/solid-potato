[project]
name = "solid-potato"
version = "0.1.0"
description = "ARGUS — SOC log collection, risk scoring, and alerting system"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aio-pika>=9.6.2",
]
